# 【姓名待填写】

> 求职意向：**AI Agent / LLM 应用开发工程师**（校招）　|　期望城市：【待填写】

| 联系方式 | 信息 |
| --- | --- |
| 手机 | 【待填写】 |
| 邮箱 | 【待填写】 |
| GitHub | 【待填写】 |
| 个人主页 / 博客 | 【待填写，可选】 |

---

## 教育背景

**【学校名称待填写】**　【专业待填写】　【本科 / 硕士】　　2022.09 – 2026.06

- GPA：【待填写】/4.0（或 4.5 制全称）；专业排名：【待填写】
- 主修课程：数据结构与算法、操作系统、计算机网络、数据库系统、机器学习、【待填写】
- 【奖项/奖学金待填写】

---

## 专业技能

- **AI Agent 方向**：熟悉 LLM 原生工具调用（Function Calling）循环、上下文工程与上下文压缩策略、多 Agent 协作与任务编排、Agent 运行可观测性与恢复机制
- **编程语言**：Python（熟练）、JavaScript / TypeScript（【按实际调整】）、C++（【按实际调整】）
- **框架与工具**：Pydantic v2、FastAPI、Uvicorn、pytest、Git、Linux Shell
- **模型服务**：DeepSeek Responses API、MiniMax，熟悉 `openai_responses` 协议与推理模式（reasoning）配置
- **工程素养**：模块化分层架构设计、协议版本管理、自动化测试与评测 basline 建设

---

## 项目经历

### JCode · 本地 Coding Agent（个人独立项目）

**2026.08 – 2026.09　|　个人项目，独立完成需求设计、架构设计、编码与测试**

技术栈：`Python 3.10+` · `Pydantic v2` · `FastAPI / Uvicorn` · `DeepSeek Responses API` · `MiniMax` · `pytest`

项目规模：**10 个核心模块、83 个源文件、约 8,200 行 Python 代码、100 次 Git 提交、106 个自动化测试用例、65 份设计文档**

**项目简介**：从 0 设计并实现的一个轻量本地 Coding Agent。覆盖「命令行入口 → 运行时装配 → 原生工具调用循环 → 结构化上下文构建 → 工具安全治理 → 子 Agent 编排 → 分层记忆 → 运行证据与 Checkpoint 恢复」完整链路，可通过 CLI 与本地 Web 控制台两种方式驱动。

**核心工作**：

1. **主链路架构设计**　独立完成 Agent 运行时分层设计，按 `app / runtime / context / tools / policy / state / memory / workers / evidence / providers` 十层拆分职责边界，明确「装配—执行—治理—留痕」四类关注点分离，将约 8,200 行代码控制在单模块可读范围内，避免核心链路互相耦合。

2. **原生工具调用循环**　基于 DeepSeek / MiniMax 的 `openai_responses` 协议实现原生 Function Calling 循环，注册 **17 个内置工具**（文件读写、精确文本补丁、目录列举、内容检索、Shell 执行、Todo 管理、用户澄清、Plan Mode、子 Agent 创建/通信/等待）；工具参数 Schema 由 Pydantic Model 自动生成，校验失败直接回传结构化错误，不做静默兜底或自动修正。

3. **上下文工程与分级压缩**　设计 `prefix / skill / history / working_memory / tools` 五段式结构化上下文（`ContextResult`），统一编译为 `ProviderInputSnapshot` 供 Provider 调用、预览与审计三方共用同一快照；设计四级上下文压缩策略，当可治理压力达到 95% 时触发摘要流程——先将 History 落盘为 artifact 并验签，再按固定 JSON Schema 生成摘要，校验最终容量后原子提交，**杜绝静默截断与信息丢失**。

4. **工具安全治理链**　设计并实现固定 10 步工具执行链：工具查找 → 参数校验 → 重复调用拦截 → 行为策略检查 → 权限检查 → Sandbox 检查 → 执行 → 工作区变更检测 → 结果脱敏 → 证据落盘。覆盖路径逃逸、未读先写、重复调用、Shell 超时、Sandbox 拦截、敏感信息脱敏等 7 类安全场景，保证 Agent 的每一次文件写入与命令执行都可追溯。

5. **分层记忆与子 Agent 编排**　实现 `Working_Memory`（回合内推理状态）/ `Daily Log`（过程日志）/ `Durable Memory`（长期结论）三层记忆体系，回合结束自动将过程信号整理沉淀为长期知识，并对密钥、Token、Password 等敏感特征做过滤拦截；实现 `worker` / `Explore` / `plan mode` 三类子 Agent 语义，子 Agent 继承父级工作区、工具策略与权限模型，结果回流至父级 Working Memory 而非直接成为最终回答，保证主循环对结果的最终裁决权。

6. **运行可观测与断点恢复**　每次运行落盘 `trace.jsonl`（逐事件轨迹）/ `task_state.json`（任务状态）/ `checkpoint.json`（含工作区指纹的恢复点）/ `report.json`（运行汇总）四类运行证据，支持基于 Checkpoint 的会话恢复与跨 Run 事件流审计；自建评测 Harness（replay / scorer / manifest / baseline），对 Agent 行为做可复现的回归度量。

7. **本地 Web 控制台**　基于 FastAPI + Uvicorn 实现单用户本地 Web 控制台，以本地仓库为项目单位组织工作区，支持浏览 `.jcode/sessions/` 历史会话、新建会话与继续对话、实时观察工具执行日志与上下文容量占用。

8. **工程规范与协议治理**　建立显式协议版本管理机制（Session schema v6 / Working Memory v3 / Checkpoint schema v5），明确拒绝为旧协议做迁移兼容，从源头控制技术债；配套输出 **65 份设计文档**与 **106 个自动化测试用例**，覆盖上下文预算、压缩摘要、工具守卫、Provider 连续性、Checkpoint 等核心路径。

---

## 其他项目

### 【项目名称待填写】

**时间待填写　|　角色待填写**

技术栈：【待填写】

- 【做了什么 + 结果量化，待填写】
- 【待填写】

---

## 荣誉奖项

- 【待填写：奖学金、竞赛（ACM / 天池 / Kaggle / 挑战杯等）、论文、专利、开源贡献】

---

## 自我评价

- 具备 LLM Agent 方向的**完整端到端工程实践**：不停留在调用 API 的 Demo 层，而是深入到上下文治理、工具安全边界、状态恢复与可观测性等生产级问题。
- 工程习惯严谨：坚持「先设计文档、再编码」的开发流程，对协议变更、失败语义与兜底行为有明确边界意识，拒绝用兜底逻辑掩盖设计缺陷。
- 【补充 1 条与你个人特质相关的评价，待填写】
