# 当前请求被工作记忆重复加权

## 冻结方案

完整删除重复请求字段，不新增稳定主目标，不新增约束识别，不修改 History 和摘要其他业务语义。

## 修改范围

- 当前请求只写入 user HistoryEvent。
- 删除 Working Memory 的字段、序列化、反序列化与渲染内容。
- 删除 ContextManager 的副本维护和固定容量重复计量。
- 删除 checkpoint 顶层副本。
- 删除 compact summary 对应字段和确定性摘要参数。
- 删除 Dream 子 Agent 的预设副本；Dream prompt 仍由 `ask()` 写入 History。
- 同步更新测试、项目链路和上下文治理文档。

## 失败语义

同步提升 Session、Working Memory、Checkpoint 和 compact summary 版本。旧协议直接拒绝，不迁移、不兼容、不静默过滤。

## 验证

- 断言当前请求在 Provider input 中只出现一次。
- 断言 Working Memory、checkpoint 和摘要输出不包含重复字段。
- 断言旧摘要版本被明确拒绝。
- 运行完整测试集。
