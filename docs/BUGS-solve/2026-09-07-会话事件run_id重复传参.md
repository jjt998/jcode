# 会话事件 run_id 重复传参

## 解决方案

1. 删除压缩对比 payload 内的 `run_id`，保留 `emit(..., run_id=task_state.run_id)` 作为唯一来源。
2. 在 `JCodeAgent.ask` 和运行循环的异常入口输出脱敏后的异常堆栈到标准错误。
3. 在 Web 后台运行线程的异常入口同样输出脱敏后的异常堆栈到标准错误。
4. 增加回归测试，验证压缩对比事件可正常发出，以及未处理运行时异常会打印且不泄露密钥。
