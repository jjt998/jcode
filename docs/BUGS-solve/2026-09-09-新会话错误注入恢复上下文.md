# 新会话错误注入恢复上下文

## 修复方案

Web Agent 配置不再设置 `resume=session_id`。显式恢复仍由 CLI 的 `resume` 参数触发；同一 Web 会话后续发送由 `_begin_run()` 根据当前 session 的 `run_ids` 统一执行 continuation 评估。

## 验收

- 新会话配置的 `resume` 必须为 `None`。
- 新会话首次 Provider 输入不得包含 `session_continuation`、旧 run 或旧 History。
- 已有会话的后续 run 仍执行统一恢复评估。
