# 恢复上下文携带完整工作区基线导致后续 run 爆量

## 修复日期

2026-09-09

## 修复方案

保留 `workspace.baseline()` 在本地的用途，用于 checkpoint 校验、工作区差异比较和 `changed_paths` 生成；不再将完整 baseline 放入新生成的 `resume_context`。

恢复上下文只保留面向运行决策的摘要字段，例如：

- workspace fingerprint 与 mismatch 状态；
- changed/stale paths；
- checkpoint 状态；
- execution fingerprint 差异；
- continuation 兼容性结果。

## 旧会话约定

本项目仅供个人使用。已有旧 session 不做迁移、过滤或兼容处理；验证修复时直接新建会话，按当前协议运行。

## 验收

- 新生成的 `resume_context` 不包含 `workspace_baseline`。
- workspace baseline 仍可用于本地 checkpoint 和变更比较。
- 恢复相关测试通过。
