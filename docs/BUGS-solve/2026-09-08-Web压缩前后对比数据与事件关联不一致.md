# Web压缩前后对比数据与事件关联不一致

## 解决方案

- before 从完整原始快照取值，after 从最终快照取值。
- Context、压缩对比和模型响应统一携带稳定 step_id。
- 最终容量失败审计携带压缩前后对比，时间线可以展示“压缩后仍超出窗口”。
- 前端区分缺失值与真实 0，拒绝非法比例，并逐字段展示内容差值。

## 保留项

Level 4 的 content_changes 展示和“无实际移出内容”状态文案不在本次范围内，遵循当前产品决定。
