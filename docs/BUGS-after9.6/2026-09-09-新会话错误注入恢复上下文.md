# 新会话错误注入恢复上下文

## 现象

Web 新建会话后，首次发送请求时 Provider 输入包含会话延续信息，可能表现为携带其他会话的上下文。

## 根因

`WebRunManager._config_for_session()` 对所有 Web 会话无条件设置 `resume=session_id`。最近恢复信息开始真正渲染到 `WorkingMemory` 后，空新会话也执行了恢复评估并把恢复上下文发送给模型。

## 影响

新会话与恢复会话的边界失效，存在上下文隔离风险。
