# 上下文压缩摘要短历史索引越界

## 发现日期

2026-09-07

## 问题现象

任务执行到上下文压缩阶段时报错：

```text
IndexError: list index out of range
```

堆栈指向 `ContextManager.build` 创建 `compact_summary` 事件的代码。

## 根因

代码仅判断 `ids` 非空，就直接读取 `ids[-3]`。当历史只有一或两个回合但内容很大、仍触发最高压力级别时，列表没有第三个元素，导致索引越界。
