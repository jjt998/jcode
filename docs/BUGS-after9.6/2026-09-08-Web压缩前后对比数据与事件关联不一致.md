# Web压缩前后对比数据与事件关联不一致

## 问题

Web 压缩卡的后端 before 指标曾混用最终快照，压缩事件缺少稳定 step_id，最终容量失败时也无法携带压缩审计；前端对缺失和非法指标处理不严格，内容差值还会丢失文件引用。

## 范围

本次修复上述问题。Level 4 暂不展示 content_changes，以及 Level 4 无实际移出内容仍显示“已压缩”，按当前产品要求保留。
